# CLAUDE.md

This file is a router. It holds no standalone context — every fact lives in one of the files below.

**Start here → [`AGENTS.md`](AGENTS.md).** Read it before touching any component.

## Where things live

- **[`AGENTS.md`](AGENTS.md)** — how to build: stack, scripts, layout/token system, component registry, conventions, quality gates.
- **[`CONTEXT.md`](CONTEXT.md)** — what this project is: origin (fork), domain, current state, relationship to hein-kollegen.de, deployment intent.

## Phase

This is the **relaunch** branch of a fork of `Nor1ck/five-s.de`. Current focus: **stabilise first**, then align visually to the HK design. See [`CONTEXT.md`](CONTEXT.md).

Design/motion system docs (DESIGN.md / MOTION.md / PRODUCT.md) are **deliberately not written yet** — they follow once the visual-alignment phase begins and decisions exist to record.

Single source of truth: do not duplicate context across these files — add it to the one that owns it and link.
